# Default: auto-detect text, normalize to LF in repo
* text=auto eol=lf

# CI & web files must be LF
*.yml  text eol=lf
*.yaml text eol=lf
*.sh   text eol=lf
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.md   text eol=lf

# Windows-only scripts may keep CRLF
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf

# Binary (don’t touch line endings)
*.png  binary
*.jpg  binary
*.jpeg binary
*.svg  text    # (SVG is XML; leave as text so diffs work)
*.ico  binary
